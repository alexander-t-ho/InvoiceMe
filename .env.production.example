# Production Environment Variables Template
# Copy this file and fill in the actual values for your production environment
# NEVER commit the actual .env.production file to version control

# ============================================
# BACKEND ENVIRONMENT VARIABLES
# ============================================

# Spring Profile
SPRING_PROFILES_ACTIVE=prod

# Database Configuration
# Format: jdbc:postgresql://HOST:PORT/DATABASE_NAME
# Example for Railway: jdbc:postgresql://containers-us-west-xxx.railway.app:5432/railway
# Example for Render: jdbc:postgresql://dpg-xxxxx-a.oregon-postgres.render.com:5432/invoiceme_xxxx
SPRING_DATASOURCE_URL=jdbc:postgresql://your-db-host:5432/invoiceme
SPRING_DATASOURCE_USERNAME=invoiceme
SPRING_DATASOURCE_PASSWORD=your-secure-database-password-here

# Server Configuration
SERVER_PORT=8081

# JWT Configuration
# IMPORTANT: Generate a secure random 32+ character string
# You can generate one using: openssl rand -base64 32
JWT_SECRET=your-256-bit-secret-key-change-this-in-production-minimum-32-characters
JWT_EXPIRATION=86400000

# CORS Configuration
# Comma-separated list of allowed origins (e.g., https://yourdomain.com,https://www.yourdomain.com)
# Leave empty to use localhost defaults (development only)
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ============================================
# FRONTEND ENVIRONMENT VARIABLES
# ============================================

# Backend API URL
# This should be your production backend URL (e.g., https://api.yourdomain.com or https://your-backend.railway.app)
# For Vercel, set this in the Vercel dashboard under Project Settings > Environment Variables
NEXT_PUBLIC_API_URL=https://your-backend-url.com/api/v1

# Node Environment
NODE_ENV=production

# ============================================
# OPTIONAL: AWS SNS (for WebSocket notifications)
# ============================================

# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_REGION=us-east-1
# AWS_SNS_TOPIC_ARN=arn:aws:sns:us-east-1:123456789012:invoice-updates
